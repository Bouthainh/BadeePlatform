@model List<ChildrenViewModel>

@{
    ViewData["Title"] = "ماهر -صفحة ولي الأمر ";
}

    <main>
        <div class="main-container">
            <aside class="sidebar">
                <div class="sidebar-links">
                <a asp-controller="Parent" asp-action="ParentHomePage" class="sidebar-item home-icon">
                        🏠
                    </a>
                <a asp-controller="Parent" asp-action="ViewProfile" class="sidebar-item profile-icon">
                        👤
                    </a>
                    <a href="#documents" class="sidebar-item documents-icon">
                        📄
                    </a>
                    <a href="#Masages" class="sidebar-item Email-icon">
                        ☐ 
                    </a>
                <a asp-controller="Parent" asp-action="Logout" class="sidebar-item logout">
                        🚪
                    </a>
                </div>
            </aside>

            <section class="main-content">
                <div class="page-header">
                    <h1>الصفحة الرئيسية</h1>
                    <div class="logo-corner">
                        <img src="~/images/MaherLogo.png" alt="MaherLogo">
                    </div>
                </div>

                <div class="children-section">
                    <h2>قائمة الأطفال</h2>
                    
                    <div class="children-grid">
                        <div class="add-child-card">
                        <a asp-controller="Parent" asp-action="AddChild">
                                <div class="add-icon">+</div>
                                <p>اضافة طفل</p>
                            </a>
                        </div>

                    @if (Model != null && Model.Any())
                    {
                        foreach (var child in Model)
                        {
                            <div class="child-card">
                                <div class="child-avatar">
                                    <img src="@child.IconImgPath" alt="child icon">
                                </div>
                                <div class="child-info">
                                    <h3>@child.ChildName</h3>
                                    <h3>@child.Age</h3>
                                    <a asp-controller="Parent"
                                       asp-action="ViewChildProfile"
                                       asp-route-childId="@child.ChildId">
                                        عرض الملف
                                    </a>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <p>لا يوجد أطفال مسجلين حتى الآن</p>
                    }

                        </div>
                  
                <div class="notifications-section">
                    <h2>الإشعارات</h2>
                    <div class="notifications-list">
                        <div class="notification-item">
                            <div class="notification-icon">👧</div>
                            <p>وصلت رسالة جديدة من معلمة لين</p>
                        </div>
                        <div class="notification-item">
                            <div class="notification-icon">🎯</div>
                            <p>تم اكتشاف تقدم في الذكاء البصري لدى عمر</p>
                        </div>
                        <div class="notification-item">
                            <div class="notification-icon">🔔</div>
                            <p>تقرير جديد متاح لطفلك</p>
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </main>
